# Next Operations Room

A Next.js `.webapp` package for proving that a package-shaped local app can feel
like a focused Mac utility instead of a generic browser tab.

It uses the App Router, a local API route, client-side refresh, filtering, and a
responsive dashboard layout. Open it with `Webapp.app` or run:

```sh
bun install
bun dev
```
